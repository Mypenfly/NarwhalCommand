!@Open ./tests/data/scenarios.rs
// 场景 8：Location:Block 定位 DataProcessor::new() 方法块，在其后插入新方法
!@Location Block
    pub fn new() -> Self {
!@New
    pub fn with_chunk_size(mut self, size: usize) -> Self {
        self.chunk_size = size;
        self
    }
@/Open
