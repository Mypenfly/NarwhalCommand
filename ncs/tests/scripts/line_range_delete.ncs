!@Open ./tests/data/rust_complex.rs

// 场景 1：Location 匹配 impl Default for AppConfig，Delete 删除该块
!@Location
impl Default for AppConfig {
    fn default() -> Self {
        AppConfig {
            name: "myapp".into(),
            version: "0.1.0".into(),
!@Delete
impl Default for AppConfig {
    fn default() -> Self {
        AppConfig {
            name: "myapp".into(),
            version: "0.1.0".into(),
@/Location

// 场景 2：内容匹配定位 Connection struct，替换字段
!@Location
struct Connection {
    id: u64,
    created_at: std::time::Instant,
    last_used: std::time::Instant,
!@New
    pool_size: usize,
    last_maintenance: std::time::Instant,
@/Location

// 场景 3：内容匹配定位 ConnectionPool struct
!@Location
pub struct ConnectionPool {
    config: AppConfig,
    connections: Vec<Connection>,
    active: usize,
@/Location

@/Open
