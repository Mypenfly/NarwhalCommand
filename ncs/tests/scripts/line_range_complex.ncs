!@Open ./tests/data/rust_complex.rs

// 场景 1：内容匹配定位 AppConfig struct 添加 log_config 字段
!@Location
pub struct AppConfig {
    pub name: String,
    pub version: String,
    pub features: Vec<String>,
!@New
    pub log_config: bool,
@/Location

// 场景 2：内容匹配替换测试函数
!@Location
    #[test]
    fn test_config_default() {
        let config = AppConfig::default();
        assert_eq!(config.name, "myapp");
    }
!@Delete
    #[test]
    fn test_config_default() {
        let config = AppConfig::default();
        assert_eq!(config.name, "myapp");
    }
!@New
    #[test]
    fn test_config_default() {
        let config = AppConfig::default();
        assert_eq!(config.name, "myapp");
        assert_eq!(config.version, "0.1.0");
    }
@/Location

// 场景 3：嵌套 Location 在 DataPipeline 中添加 stage_count
!@Location
impl DataPipeline {
!@Location
    pub fn execute(&self, input: &str) -> Result<String, String> {
!@New Start
    pub fn stage_count(&self) -> usize {
        self.stages.len()
    }

@/Location
@/Location

// 场景 4：嵌套定位替换 run_app 中的 pipeline 调用
!@Location
pub fn run_app(config: AppConfig) -> Result<(), String> {
!@Location
    let result = pipeline.execute(input)?;
    log::info!("pipeline completed successfully");
!@New
    log::info!("running pipeline with input: {:?}", input);
    let result = pipeline.execute(input)?;
    log::info!("pipeline completed successfully");
@/Location
@/Location

@/Open
